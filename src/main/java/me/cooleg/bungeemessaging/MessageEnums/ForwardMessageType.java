package me.cooleg.bungeemessaging.MessageEnums;

/**
 * Honestly pretty useless enum. Only exists because being able to instantiate a class only for these types of messages,
 * with a non-compatible message type, feels like something I shouldn't let happen.
 */
public enum ForwardMessageType {
    FORWARD,
    FORWARDTOPLAYER;
}
